# パソコンちぇっ君 詳細仕様書

## 1. 文書情報

| 項目 | 内容 |
|---|---|
| 文書名 | パソコンちぇっ君 詳細仕様書 |
| 対象バージョン | 0.3.0 |
| 対象OS | Windows |
| アプリケーション種別 | デスクトップアプリケーション |
| 作成日 | 2026年9月17日 |
| 開発・提供 | Alpha Script |
| リポジトリ | https://github.com/AlphaScript-kyoto/PC-Health-Checker |

本書は、現行ソースコードを基準として「パソコンちぇっ君」の目的、機能、画面、判定条件、内部構成、API、データ、運用方法を定義する。

---

## 2. システム概要

### 2.1 目的

「パソコンちぇっ君」は、Windows PCの状態を初心者にも分かりやすく見える化し、ディスク故障や空き容量不足への早めの対応を支援するツールである。

主な目的は次のとおり。

1. PC、物理ディスク、論理ドライブの情報を一画面で確認できるようにする
2. SMART情報や空き容量からリスクを判定する
3. ディスク交換が必要な場合に、交換候補を提示する
4. フォルダの使用容量を可視化し、整理候補を案内する
5. PCパーツ価格を記録し、買い時の判断材料を提供する
6. PC関連ニュースをアプリ内で確認できるようにする
7. 毎日の自動スキャンと通知により、異常の見落としを減らす

### 2.2 重要な安全方針

- アプリ内からファイルを自動削除しない
- 商品を自動購入しない
- 容量マップの削除候補は、削除可否の判断材料のみを提示する
- システム領域は危険として明示し、直接削除を促さない
- シリアル番号は初期状態でマスクして表示する
- 外部リンクはElectron内に表示せず、既定のブラウザで開く

### 2.3 対象範囲

本システムが対象とする機能は次のとおり。

- PC構成情報の収集
- ディスク情報およびSMART情報の収集
- ドライブ空き容量の収集
- 健康リスクの評価
- スキャン履歴およびアラートの保存
- 容量マップの作成
- 交換候補の提示
- パーツ価格の追跡
- PC関連ニュースの取得
- Windows通知
- Windowsスタートアップ登録
- タスクトレイ常駐
- 管理者権限での再起動

### 2.4 対象外

- macOSおよびLinuxの正式対応
- ファイルの削除、移動、圧縮
- ディスクの修復、フォーマット、パーティション変更
- SMARTセルフテストの実行
- 商品の注文、決済、在庫確保
- ウイルス・マルウェア検査
- CPUやGPUへの負荷テスト
- クラウドへの診断結果の同期

---

## 3. 利用者と利用条件

### 3.1 想定利用者

- PCの故障兆候を簡単に確認したい一般利用者
- ディスク容量不足の原因を調べたい利用者
- PCパーツの交換時期や価格を確認したい利用者
- SMARTの詳細値を確認したい中級者以上の利用者

### 3.2 動作環境

#### 開発環境

- Windows
- Node.js 18以上
- Python 3.11以上を推奨
- npm
- 任意: smartmontoolsの `smartctl`

#### 配布版

- Windows x64
- ポータブル実行ファイルを起動できること
- SMART詳細取得時は管理者権限および `smartctl` を推奨

### 3.3 ネットワーク

次の機能はインターネット接続を必要とする。

- ニュース取得
- 価格情報の更新
- 価格.com、Amazon、Keepa等の外部ページ表示

PC診断、履歴表示、容量マップ作成はローカルで動作する。ただし、未取得の外部情報はオフライン時に更新できない。

---

## 4. システム構成

### 4.1 全体構成

本システムは、次の3層で構成する。

1. **デスクトップ層**
   - Electron
   - ウィンドウ、タスクトレイ、単一起動、管理者昇格、外部リンク、バックエンド起動を担当
2. **表示層**
   - React + TypeScript + Vite
   - 各画面の表示、操作受付、進捗表示を担当
3. **処理・データ層**
   - Python + FastAPI + SQLite
   - PC情報収集、SMART取得、判定、容量走査、永続化、通知、外部情報取得を担当

### 4.2 通信方式

- React UIからFastAPIへHTTP/JSONで通信する
- 接続先は `http://127.0.0.1:8787`
- バックエンドはループバックアドレスのみで待ち受ける
- Electron固有機能はpreload経由のIPCで呼び出す
- RendererではNode.js統合を無効化し、context isolationを有効化する

### 4.3 起動構成

1. Electronが単一起動ロックを取得する
2. タスクトレイとメインウィンドウを作成する
3. Pythonバックエンドを子プロセスとして起動する
4. `/api/health` を最大45秒待機する
5. React UIがローカルAPIから状態を取得する
6. 開発時はVite開発サーバーを優先し、利用できない場合は `dist/index.html` を表示する

---

## 5. 機能要件

### 5.1 ホーム

#### 表示内容

- 総合ステータス
- 最終スキャン日時
- PC構成情報
- ディスクおよびドライブの概要
- 現在のアラート
- 管理者権限の有無
- `smartctl` の利用可否
- 健康診断と容量マップの進捗

#### 操作

- 今すぐスキャン
- スキャンの中断
- スキャンの再開
- 最初から再スキャン
- 管理者として再起動

#### スキャン順序

全体スキャンは次の順で実行する。

1. PC情報の取得
2. ディスク情報・SMARTの取得
3. ドライブ空き容量の取得
4. リスク判定と交換提案の作成
5. 診断結果と履歴の保存
6. 各ドライブの容量マップ作成

健康診断と容量マップは同時実行しない。HDDでランダムアクセスが競合し、極端に遅くなることを避けるため、健康診断完了後に容量マップを実行する。

### 5.2 ディスク

#### 一覧表示

- デバイスID
- モデル名
- 容量
- メディア種別
- バス種別
- 健康状態
- リスクレベル
- 関連ボリューム
- 空き容量
- 判定理由

#### 詳細ウィンドウ

ディスクごとに別ウィンドウを1つだけ開く。同じディスクの詳細を再度開いた場合は、既存ウィンドウを前面に表示する。

表示対象は次のとおり。

- モデル
- 容量
- 健康状態
- 温度
- ファームウェア
- シリアル番号
- インターフェース
- 転送モード
- ATA規格、SATAバージョン
- 対応機能
- バッファサイズ
- NVキャッシュ
- 回転数
- フォームファクター
- 総書込み量
- 電源投入回数
- 使用時間
- SMART属性一覧

取得できない項目は空欄または `----` 相当で扱う。非管理者または `smartctl` 未導入の場合、一部のSMART項目を取得できないことがある。

### 5.3 容量マップ

#### 対象

- Windowsが認識する固定ドライブ
- Windowsが認識するリムーバブルドライブ

#### 表示内容

- ドライブ総容量
- 使用量
- 空き容量
- フォルダのツリーマップ
- 走査ファイル数
- 走査フォルダ数
- 現在の走査パス
- スキップ件数
- 処理時間
- 整理候補
- 安全性ラベルと理由

#### 安全性ラベル

| ラベル | 意味 | 例 |
|---|---|---|
| `safe` | 通常は再生成可能な整理候補 | ごみ箱、一時ファイル、ブラウザキャッシュ |
| `caution` | 内容を確認してから判断する場所 | Downloads、ユーザーデータ、Windows.old |
| `danger` | 削除すると重大な問題が起きる可能性がある場所 | Windows、System32、Program Files、Recovery |
| `neutral` | 自動判定できない場所 | 上記ルールに該当しないフォルダ |

整理候補は50MiB以上の対象フォルダから最大30件を容量順で返す。

#### 走査除外・省略

- シンボリックリンク
- ジャンクションおよび再解析ポイント
- OneDrive等のオンライン専用プレースホルダ
- アクセス不能な項目
- 一部のWindows互換リンク
- `$` で始まる一部システム領域
- `WinSxS` および `CSC` の詳細再帰

`WinSxS` 等は処理時間と重複集計を抑えるため、浅い階層のみを概算する。

#### 操作

- ドライブを指定して開始
- 一時停止
- 続きから再開
- 現在の結果を破棄して最初から再走査
- 対象フォルダをエクスプローラーで開く

本機能からファイル削除は行わない。

### 5.4 提案

リスクが `ReplaceSoon` または `Critical` のディスクを交換対象として扱う。

提案内容は次の設定を考慮する。

- 予算上限
- 新品、中古、どちらでもよい
- SSD、HDD、どちらでもよい
- 希望容量
- 速度、静音性、容量、価格の優先度

提案は検索リンクおよび参考情報であり、価格、互換性、在庫、購入可否を保証しない。

### 5.5 価格

- パーツカタログをカテゴリおよびブランド単位で表示する
- 追跡対象を利用者が選択できる
- 価格.comおよびAmazon由来の価格履歴を保存・表示する
- Amazon ASINを取得できた場合はKeepaの1年グラフを表示する
- カタログ更新で消えた追跡対象は、維持または削除を選択できる
- 手動更新および定期更新に対応する

外部サイトの仕様変更、通信障害、アクセス制限により価格を取得できない場合がある。表示価格は参考値とする。

### 5.6 ニュース

- PC関連ニュースのタイトル、配信元、公開日時、概要、画像を表示する
- ニュースタブを開いた際に最新情報を取得する
- 強制再取得要求に対応する
- 記事リンクは既定ブラウザで開く

### 5.7 設定

| 設定 | 初期値 | 許容値・説明 |
|---|---:|---|
| 通知 | 有効 | 有効・無効 |
| 空き容量警告 | 10% | 1〜50% |
| 空き容量危険 | 5% | 0.5〜30% |
| 予算上限 | 30,000円 | 1,000〜500,000円 |
| 新品・中古 | どちらでも | `new` / `used` / `either` |
| メディア種別 | SSD | `ssd` / `hdd` / `either` |
| 希望容量 | 2TB | 0.1〜20TB |
| 優先項目 | 速度 | `speed` / `quiet` / `capacity` / `price` |
| 毎日のスキャン時刻 | 09:00 | 24時間表記の `HH:MM` |
| Windows起動時に開始 | 無効 | 有効・無効 |

### 5.8 自動スキャン

- アプリが常駐している間、設定時刻を30秒以内の間隔で確認する
- 指定時刻以降、その日に未実行の場合のみ開始する
- 指定時刻を過ぎてアプリを起動した場合、起動時スキャンを当日分とみなし、定刻スキャンを重複実行しない
- 他の全体スキャンが実行中の場合は、その完了を待つ
- 完了待ちの上限は45分とする
- 完了後に価格の定期更新処理を確認する
- 完了通知を表示する

### 5.9 通知

- ディスクまたは容量の異常をWindows通知で知らせる
- `Watch`、`ReplaceSoon`、`Critical` を通知対象とする
- 通知設定が無効の場合は通知しない
- 直近の同一アラートは重複通知しない
- 通知できなかった場合も診断処理自体は継続する

### 5.10 タスクトレイ

タスクトレイから次の操作を提供する。

- ウィンドウを表示
- 今すぐスキャン
- 管理者として再起動
- 終了

メインウィンドウの閉じるボタンではアプリを終了せず、ウィンドウを非表示にして常駐を継続する。終了はトレイメニューから行う。

### 5.11 管理者として再起動

- Windows UACを利用してElectronを管理者権限で再起動する
- 新しい管理者プロセスの準備完了後に旧プロセスを終了する
- UACがキャンセルされた場合は非管理者状態で処理を継続する
- 開発時は最新UIをビルドしてから管理者プロセスへ引き継ぐ
- すでに管理者の場合はUIを再ビルドし、キャッシュを無視して再読込する

---

## 6. 健康状態の判定仕様

### 6.1 ステータス

| ステータス | 重要度 | 意味 |
|---|---:|---|
| `OK` | 0 | 明確な問題を検出していない |
| `Watch` | 1 | 経過観察または早めの整理を推奨 |
| `ReplaceSoon` | 2 | 交換準備または早急な対処を推奨 |
| `Critical` | 3 | データ退避と即時対応を強く推奨 |
| `Unknown` | - | 未スキャンまたは状態を判定できない |

複数の条件に該当した場合は、最も重要度の高いステータスを採用する。

### 6.2 空き容量

- 空き容量が危険しきい値以下: `Critical`
- 空き容量が警告しきい値以下: `Watch`
- 既定値は危険5%、警告10%
- 物理ディスクに複数ボリュームがある場合は、最も空き率の低い関連ボリュームを代表値とする

### 6.3 OSおよびSMART

| 条件 | 判定 |
|---|---|
| OS報告が `unhealthy` | `Critical` |
| OS報告が `warning` | `ReplaceSoon` |
| SMART総合判定が `FAILED` | `Critical` |
| 代替処理済セクタが1〜49 | `ReplaceSoon` |
| 代替処理済セクタが50以上 | `Critical` |
| 保留中セクタが1以上 | `Critical` |
| 訂正不能セクタが1以上 | `Critical` |
| メディアエラーが1以上 | `ReplaceSoon` |
| SSD摩耗度が90〜94% | `ReplaceSoon` |
| SSD摩耗度が95%以上 | `Critical` |

### 6.4 温度

- 60℃以上: `Watch`
- 70℃以上: `ReplaceSoon`

温度判定のみでは `Critical` にはしないが、他の条件との組み合わせでは総合状態が `Critical` になる場合がある。

### 6.5 通電時間

- 5年以上相当: `Watch`
- 8年以上相当: `ReplaceSoon`
- 1年は8,760時間として換算する

### 6.6 既知の旧型モデル

設定済みの旧型モデル文字列に一致した場合は `ReplaceSoon` とし、モデル固有の注意文を表示する。

### 6.7 ディスク構成の変化

- 未登録のディスクを検出した場合: `Watch` のアラート
- 前回まで存在したディスクが消えた場合: `Watch` のアラート
- 総合状態が `OK` の場合は `Watch` に引き上げる

---

## 7. スキャン状態管理

### 7.1 全体進捗

全体進捗は次の重みで合成する。

- 健康診断: 55%
- 容量マップ: 45%

### 7.2 主なフェーズ

- `idle`: 未実行
- `queued`: 開始待ち
- `inventory`: PC情報取得
- `disks`: ディスク・SMART取得
- `volumes`: 空き容量取得
- `evaluate`: リスク判定
- `save`: 保存
- `space_map`: 容量マップ作成
- `paused`: 一時停止
- `done`: 完了
- `cancelled`: 中止
- `error`: エラー

### 7.3 一時停止

- 健康診断は処理区切りのチェックポイントで停止する
- 容量マップは走査ループのチェックポイントで停止する
- OS APIや外部コマンドの実行中は、呼び出し完了まで停止が反映されない場合がある
- 再開時は保持している途中状態から処理を続行する

### 7.4 最初から再スキャン

1. 現在のスキャンにキャンセル要求を出す
2. 実行スレッドの終了を待つ
3. 停止完了後、新しい全体スキャンを開始する
4. 15秒以内に停止しない場合は、再試行を求めるメッセージを返す

---

## 8. API仕様

### 8.1 共通仕様

- Base URL: `http://127.0.0.1:8787`
- データ形式: JSON
- APIドキュメント: `/api/docs`
- 正常時: 主にHTTP 200
- 入力不正: HTTP 400
- 容量スキャンキャンセル済み: HTTP 409
- 容量スキャン内部エラー: HTTP 500

### 8.2 エンドポイント一覧

| Method | Path | 概要 |
|---|---|---|
| GET | `/api/health` | バックエンド稼働確認 |
| GET | `/api/about` | アプリ情報、権限、smartctl状態 |
| GET | `/api/status` | 最新の診断結果 |
| POST | `/api/scan` | 全体スキャン開始 |
| POST | `/api/scan/pause` | 全体スキャン一時停止 |
| POST | `/api/scan/resume` | 全体スキャン再開 |
| POST | `/api/scan/restart` | 全体スキャンを最初から再実行 |
| GET | `/api/scan/progress` | 全体スキャン進捗 |
| POST | `/api/scan/sync` | 完了まで待つ互換用スキャン |
| GET | `/api/settings` | 設定取得 |
| PUT | `/api/settings` | 設定更新 |
| GET | `/api/alerts` | 最近のアラート取得 |
| GET | `/api/disks/{device_id}/history` | ディスク履歴取得 |
| GET | `/api/recommendations` | 交換提案取得 |
| GET | `/api/news` | ニュース取得 |
| GET | `/api/prices` | 価格追跡状態取得 |
| PUT | `/api/prices/tracked` | 追跡対象更新 |
| POST | `/api/prices/orphans` | カタログ外項目の扱いを決定 |
| POST | `/api/prices/refresh` | 価格更新 |
| GET | `/api/drives` | 容量マップ対象ドライブ取得 |
| POST | `/api/space/scan` | 単独容量スキャン開始 |
| POST | `/api/space/pause` | 単独容量スキャン一時停止 |
| POST | `/api/space/resume` | 単独容量スキャン再開 |
| POST | `/api/space/restart` | 単独容量スキャン再実行 |
| GET | `/api/space/progress` | 単独容量スキャン進捗 |
| GET | `/api/space/result` | 単独容量スキャン結果 |
| POST | `/api/space/cancel` | 単独容量スキャン中止 |

### 8.3 主要なリクエスト例

#### 設定更新

```json
{
  "notify_enabled": true,
  "capacity_warn_pct": 10,
  "capacity_critical_pct": 5,
  "daily_scan_time": "09:00"
}
```

#### 価格追跡対象更新

```json
{
  "ids": ["catalog-item-id-1", "catalog-item-id-2"]
}
```

#### 容量スキャン開始

```json
{
  "rootPath": "C:\\"
}
```

### 8.4 最新状態の取得

`GET /api/status` は次の優先順位で返す。

1. 現在のプロセスが保持する最新スキャン結果
2. SQLiteに保存された最新スナップショット
3. 未スキャンを示す `Unknown`

---

## 9. Electron IPC仕様

Rendererに公開する機能は次の5つに限定する。

| IPC機能 | 説明 |
|---|---|
| `isAdmin` | 現在の管理者権限を確認 |
| `elevate` | 管理者として再起動 |
| `openPath` | エクスプローラー等でパスを開く |
| `getBackendUrl` | バックエンドURLを取得 |
| `openDiskDetail` | ディスク詳細ウィンドウを開く |

`__other__` を含む容量マップの集約項目は実在パスではないため、直接開かない。

---

## 10. データ仕様

### 10.1 保存場所

| 環境 | 保存先 |
|---|---|
| 開発 | `backend/app/data/` |
| 配布版 | `%LOCALAPPDATA%\PCHealthChecker\data` |

主データベース名は `health.db` とする。

### 10.2 SQLiteテーブル

| テーブル | 用途 | 主な項目 |
|---|---|---|
| `settings` | アプリ設定 | key, value |
| `snapshots` | 診断結果全体 | created_at, overall_status, payload_json |
| `disk_history` | ディスク状態履歴 | device_id, health_status, free_pct, risk_level, smart_json |
| `alerts` | アラート履歴 | level, title, message, device_id, notified |
| `known_disks` | 接続ディスクの追跡 | device_id, model, serial, first_seen, last_seen |
| `tracked_parts` | 価格追跡対象 | catalog_id, keep_legacy, pending_decision |
| `price_history` | 価格履歴 | catalog_id, price_yen, source, url, fetched_at |
| `app_meta` | カタログ版等の内部情報 | key, value |

### 10.3 日時

- バックエンドの保存日時はISO 8601形式とする
- 診断および履歴の保存日時は原則UTCとする
- 毎日の実行時刻はPCのローカル時刻として扱う

### 10.4 保持件数

- ディスク履歴APIは既定で最新50件を返す
- アラートAPIは既定で最新30件を返す
- 現行実装では古いスナップショット、履歴、アラートの自動削除を行わない

---

## 11. 非機能要件

### 11.1 セキュリティ

- APIは `127.0.0.1` のみに公開する
- Rendererの `nodeIntegration` は無効とする
- Rendererの `contextIsolation` は有効とする
- OS操作はpreloadで公開した限定APIを経由する
- 外部URLは既定ブラウザで開く
- 管理者権限は必要時に利用者のUAC承認を得る
- シリアル番号は画面上で初期マスクする

注意事項として、FastAPIのCORS設定は現行実装で全オリジンを許可している。待受先はローカル限定だが、将来ネットワーク公開する場合は許可オリジンを制限すること。

### 11.2 性能

- バックエンド起動待ちは最大45秒
- 進捗情報を定期的にUIへ返せること
- 容量走査中は約2秒ごとにハートビートを更新する
- 巨大または循環しやすいシステム領域は詳細走査を省略する
- ツリーマップの子要素数を階層ごとに制限する
- HDD負荷軽減のため、SMART取得と全ファイル走査は順次実行する

### 11.3 信頼性

- SQLite更新時に例外が起きた場合はロールバックする
- バックエンド異常終了時はログを残す
- 通知や一部外部情報の失敗で診断全体を停止しない
- 二重起動を禁止し、既存ウィンドウを前面表示する
- 終了時はPython子プロセスも停止し、ポートを解放する

### 11.4 可用性

- メインウィンドウを閉じてもタスクトレイで常駐する
- Viteに接続できない場合はビルド済みUIへフォールバックする
- SMART詳細を取得できない場合でも、取得可能なOS情報と容量情報で画面を利用できる

### 11.5 ログ

| 環境・用途 | 保存先 |
|---|---|
| 開発バックエンド起動 | `backend/app/data/backend-launch.log` |
| 配布版バックエンド起動 | `%LOCALAPPDATA%\PCHealthChecker\backend-launch.log` |
| 開発ランチャー | `%LOCALAPPDATA%\PCHealthChecker\dev-launch.log` |

ログには起動日時、バックエンドの標準出力、標準エラー等を記録する。SMART情報やPC情報を記録する処理を追加する場合は、個人情報の混入に注意すること。

---

## 12. エラー処理

| 状況 | 動作 |
|---|---|
| バックエンドが45秒以内に起動しない | 通知を表示し、ウィンドウは開いた状態にする |
| スキャン中に再度開始 | 新規開始せず `already running` を返す |
| 実行中でないスキャンを一時停止 | 操作不可メッセージを返す |
| 実行中でないスキャンを再開 | 操作不可メッセージを返す |
| 容量走査対象が読めない | エラーとしてAPIへ返す |
| ファイル・フォルダへのアクセス拒否 | 対象をスキップして走査を継続する |
| UACをキャンセル | 非管理者状態へ戻り、利用を継続する |
| 外部リンクを開けない | エラーダイアログを表示する |
| 同じアプリを二重起動 | 既存起動を案内し、新規プロセスを終了する |
| 通知に失敗 | 通知済みフラグを立てず、診断結果は保存する |

---

## 13. ビルド・配布仕様

### 13.1 開発起動

```powershell
npm run dev
```

Vite、Electron、Pythonバックエンドを開発構成で起動する。

### 13.2 UIビルド

```powershell
npm run build
npm start
```

TypeScript型検査後にViteでUIとElectronコードをビルドする。

### 13.3 配布版ビルド

```powershell
npm run release
```

次を順に作成する。

1. React UIおよびElectronコード
2. PyInstallerで固めたPythonバックエンド
3. Electron BuilderによるWindows x64ポータブル版

### 13.4 配布物

- `PC-Chekkun-0.3.0-portable.exe`
- `run_as_admin.bat`
- `readme.txt`

---

## 14. ファイル構成と責務

| パス | 責務 |
|---|---|
| `electron/main.ts` | Electronライフサイクル、ウィンドウ、トレイ、昇格、バックエンド管理 |
| `electron/preload.ts` | Rendererへ公開する安全なIPC API |
| `src/App.tsx` | メイン画面構成とタブ制御 |
| `src/api.ts` | FastAPIクライアント |
| `src/types.ts` | UIで使用するデータ型 |
| `src/pages/HomePage.tsx` | 総合状態とスキャン操作 |
| `src/pages/DisksPage.tsx` | ディスク一覧 |
| `src/pages/SpacePage.tsx` | 容量マップ |
| `src/pages/RecommendationsPage.tsx` | 交換提案 |
| `src/pages/PricesPage.tsx` | 価格追跡 |
| `src/pages/NewsPage.tsx` | ニュース |
| `src/pages/SettingsPage.tsx` | 設定 |
| `src/components/DiskDetailView.tsx` | ディスク詳細 |
| `src/components/Treemap.tsx` | 容量ツリーマップ |
| `backend/app/main.py` | FastAPI起動と毎日スケジューラ |
| `backend/app/api/__init__.py` | HTTP API |
| `backend/app/scanner.py` | 全体スキャンと進捗管理 |
| `backend/app/engine/risk.py` | 健康リスク判定 |
| `backend/app/space_scan.py` | フォルダ容量走査 |
| `backend/app/db.py` | SQLite初期化とデータ操作 |
| `backend/app/collectors/` | PC、ディスク、容量の情報収集 |
| `backend/app/recommend/` | 交換候補生成 |
| `backend/app/price_tracker.py` | 価格取得と履歴管理 |
| `backend/app/news.py` | ニュース取得 |
| `backend/app/notify.py` | Windows通知 |
| `backend/app/startup.py` | Windowsスタートアップ管理 |
| `scripts/build_backend.ps1` | Pythonバックエンドの配布用ビルド |
| `scripts/build_release.ps1` | 配布物の一括生成 |

---

## 15. 制約・既知事項

1. SMART情報の取得可否と項目数は、ディスク、接続方式、権限、ドライバー、`smartctl` の有無に依存する
2. USB変換アダプター経由ではSMARTを取得できない場合がある
3. 容量マップはファイルシステム上のアクセス可能な項目のみ集計する
4. ハードリンク、アクセス拒否、クラウドファイル、走査省略領域により、Windowsの使用量とツリーマップ合計が一致しない場合がある
5. 大容量HDDや大量の小ファイルがある環境では、容量マップ作成に長時間かかる
6. 価格およびニュースの正確性・完全性・継続取得は外部サービスに依存する
7. 健康判定は故障を確実に予測するものではない。重要データの定期バックアップが必要である
8. 現行バージョンには診断履歴の自動削除上限がない
9. APIはローカル利用を前提としており、外部ネットワーク公開を想定しない

---

## 16. 受入確認項目

### 16.1 起動

- [ ] 開発コマンドでメインウィンドウが表示される
- [ ] FastAPIのヘルスチェックが成功する
- [ ] 二重起動時に既存アプリが前面表示される
- [ ] ウィンドウを閉じてもトレイに常駐する
- [ ] トレイの終了でElectronとPythonが終了する

### 16.2 診断

- [ ] PC情報を取得できる
- [ ] 物理ディスクと論理ドライブを表示できる
- [ ] SMART取得可否を表示できる
- [ ] 空き容量しきい値に応じて状態が変化する
- [ ] 診断結果、ディスク履歴、アラートがSQLiteへ保存される
- [ ] 健康診断後に容量マップが開始される

### 16.3 操作

- [ ] スキャンを一時停止できる
- [ ] 一時停止したスキャンを再開できる
- [ ] 最初から再スキャンできる
- [ ] 管理者として再起動できる
- [ ] UACキャンセル後も通常利用できる

### 16.4 容量マップ

- [ ] ドライブ一覧を取得できる
- [ ] 走査進捗が更新される
- [ ] ツリーマップと整理候補が表示される
- [ ] 危険なシステム領域が `danger` になる
- [ ] ファイル削除処理が存在しない

### 16.5 外部情報

- [ ] ニュースを取得・表示できる
- [ ] 価格追跡対象を保存できる
- [ ] 価格履歴を表示できる
- [ ] 外部リンクが既定ブラウザで開く

---

## 17. 今後の改善候補

本章は現行仕様ではなく、将来検討項目である。

- 履歴データの保持期間または最大件数の設定
- 診断結果のJSONまたはCSVエクスポート
- バックアップ実施状況の確認機能
- SMART値の時系列グラフ
- 診断条件の詳細なユーザーカスタマイズ
- APIのCORS許可元制限
- 自動テストおよび配布版E2Eテストの拡充
- アクセシビリティとキーボード操作の検証
- 外部価格取得失敗時の理由表示強化

